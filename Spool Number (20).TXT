SQL> /* PARA FAZER COMENTARIO DE UMA LINHA USAR -- E MAIS LINHAS '/*' PARA LIMPAR TELA USAR COMANDO 'CLEAR SCREEN'
SQL> A CRIACAO DE ARQUIVOS NO CMD USA O COMANDO 'SPOOL' C:\PASTADESTINARIO */
SQL> CREATE TABLE CURSO ()
  2  
SQL> CREATE TABLE CURSO
  2  (SG_CURSO VARCHAR(5) PRIMARY KEY,
  3  NM_CURSO VARCHAR(50) NOT NULL,);
SP2-0640: Não conectado
SQL> (SG_CURSO VARCHAR(5) PRIMARY KEY,
  2  CREATE TABLE CURSO (
  3  SG_CURSO VARCHAR(5) PRIMARY KEY,
  4  NM_CURSO VARCHAR(50) NOT NULL);
SP2-0640: Não conectado
SQL> CREATE TABLE CURSO (
  2  (SG_CURSO VARCHAR(5) PRIMARY KEY,
  3  NM_CURSO VARCHAR(50) NOT NULL,);
SP2-0640: Não conectado
SQL> --COMANDO SAVE PARA SALVAR ARQUIVO COMO SQL 'SAVE C:\USER\AULA.SQL'--
SQL> CONNECT / AS SYSDBA
ERROR:
ORA-01031: insufficient privileges 


SQL> 
SQL> CLEAR SCREEN

SQL> SPOOL OFF
