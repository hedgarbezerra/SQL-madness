SQL> CREATE TABLE DEPARTAMENTO(
  2  ID_DEPT NUMBER(7),
  3  NOME VARCHAR(25),
  4  CONSTRAINT PK_DEPARTAMENTO PRIMARY KEY (ID_DEPT));

Tabela criada.

SQL> CLEAR
SQL> SELECT * FROM ALL_SEQUENCES WHERE SEQUENCE_NAME = 'SEQUENCE';

não há linhas selecionadas

SQL> SELECT * FROM ALL_SEQUENCES WHERE SEQUENCE_NAME = 'DEPARTAMENTO';

não há linhas selecionadas

SQL> SELECT * FROM ALL_SEQUENCES WHERE SEQUENCE_NAME = 'DEPT';

não há linhas selecionadas

SQL> SELECT * FROM ALL_SEQUENCES WHERE SEQUENCE_NAME LIKE 'DEPT';

não há linhas selecionadas

SQL> SELECT * FROM ALL_SEQUENCES WHERE SEQUENCE_NAME LIKE 'DE';

não há linhas selecionadas

SQL> SELECT * FROM ALL_SEQUENCES WHERE SEQUENCE_NAME LIKE 'A';

não há linhas selecionadas

SQL> CREATE SEQUENCE
  2  START WITH 10
  3  INCREMENT BY 10
  4  
SQL> CREATE SEQUENCE DEPT_HED
  2  START WITH 10
  3  INCREMENT BY 10
  4  ORDER;

Seqüência criada.

SQL> INSERT INTO DEPARTAMENTO (ID_DEPT,NOME) VALUES (DEPT_HED.NEXTVAL,'ADM');

1 linha criada.

SQL> INSERT INTO DEPARTAMENTO (ID_DEPT,NOME) VALUES (DEPT_HED.NEXTVAL,'TI');

1 linha criada.

SQL> INSERT INTO DEPARTAMENTO (ID_DEPT,NOME) VALUES (DEPT_HED.NEXTVAL,'AVG');

1 linha criada.

SQL> CREATE TABLE EMPREGADO(
  2  ID_EMRPEGADO NUMBER(7),
  3  NOME VARCHAR(2),
  4  
SQL> CREATE TABLE EMPREGADO(
  2  ID_EMRPEGADO NUMBER(7),
  3  NOME VARCHAR(25),
  4  SOBRENOME VARCHAR(25),
  5  DEPTID NUMBER(7),
  6  CONSTRAINT FK_EMPREGADO_DEPT FOREIGN KEY (DEPTID) REFERENCES DEPARTAMENTO;
CONSTRAINT FK_EMPREGADO_DEPT FOREIGN KEY (DEPTID) REFERENCES DEPARTAMENTO
                                                                        *
ERRO na linha 6:
ORA-00907: parêntese direito não encontrado 


SQL> CREATE TABLE EMPREGADO(
  2  ID_EMRPEGADO NUMBER(7),
  3  NOME VARCHAR(25),
  4  SOBRENOME VARCHAR(25),
  5  DEPTID NUMBER(7),
  6  CONSTRAINT FK_EMPREGADO_DEPT FOREIGN KEY (DEPTID) REFERENCES DEPARTAMENTO);

Tabela criada.

SQL> ALTER TABLE EMPREGADO MODIFY SOBRENOME VARCHAR(30);

Tabela alterada.




SQL> CREATE TABLE EMPREGADO2(
  2  ID_EMPREGADO NUMBER(7),
  3  NOME_EMPREGADO VARCHAR(50),
  4  DEPT_ID NUMBER (7),
  5  CONSTRAINT PK_EMPREGADO2 PRIMARY KEY (ID_EMPREGADO));

Tabela criada.

SQL> DROP TABLE EMPREGADO;

Tabela eliminada.

SQL> FLASHBACK TABLE EMPREGADO TO BEFORE DROP;
FLASHBACK TABLE EMPREGADO TO BEFORE DROP
*
ERRO na linha 1:
ORA-38305: objeto não está na LIXEIRA 


SQL> SELECT ORIGINAL_NAME,OPERATION,DROPTIME FROM RECYCLEBIN;

não há linhas selecionadas

SQL> ALTER SESSION SET RECYCLEBIN = ON;

Sessão alterada.

SQL> SELECT ORIGINAL_NAME,OPERATION,DROPTIME FROM RECYCLEBIN;

não há linhas selecionadas

SQL> SELECT ORIGINAL_NAME,OPERATION,DROPTIME FROM RECYCLEBIN;

não há linhas selecionadas

SQL> CREATE TABLE EMPREGADO(
  2  ID_EMRPEGADO NUMBER(7),
  3  NOME VARCHAR(25),
  4  SOBRENOME VARCHAR(25),
  5  DEPTID NUMBER(7),
  6  CONSTRAINT FK_EMPREGADO_DEPT FOREIGN KEY (DEPTID) REFERENCES DEPARTAMENTO;
CONSTRAINT FK_EMPREGADO_DEPT FOREIGN KEY (DEPTID) REFERENCES DEPARTAMENTO
                                                                        *
ERRO na linha 6:
ORA-00907: parêntese direito não encontrado 


SQL> CREATE TABLE EMPREGADO(
  2  ID_EMRPEGADO NUMBER(7),
  3  NOME VARCHAR(25),
  4  SOBRENOME VARCHAR(25),
  5  DEPTID NUMBER(7),
  6  CONSTRAINT FK_EMPREGADO_DEPT FOREIGN KEY (DEPTID) REFERENCES DEPARTAMENTO);

Tabela criada.

SQL> DROP TABLE EMPREGADO;

Tabela eliminada.

SQL> SELECT ORIGINAL_NAME,OPERATION,DROPTIME FROM RECYCLEBIN;

não há linhas selecionadas

SQL> SELECT ORIGINAL_NAME,OPERATION,DROPTIME FROM USER_RECYCLEBIN;

não há linhas selecionadas

SQL> SELECT ORIGINAL_NAME,OPERATION,DROPTIME FROM DBA_RECYCLEBIN;

não há linhas selecionadas

SQL> SELECT * FROM DBA_RECYCLEBIN;

não há linhas selecionadas

SQL> SELECT * FROM USER_RECYCLEBIN;

não há linhas selecionadas

SQL> SHOW PARAMETER RECYCLEBIN;

NAME                                 TYPE        VALUE                          
------------------------------------ ----------- ------------------------------ 
recyclebin                           string      ON                             
SQL> CREATE TABLE EMPREGADO(
  2  ID_EMRPEGADO NUMBER(7),
  3  NOME VARCHAR(25),
  4  SOBRENOME VARCHAR(25),
  5  CONSTRAINT FK_EMPREGADO_DEPT FOREIGN KEY (DEPTID) REFERENCES DEPARTAMENTO);
CONSTRAINT FK_EMPREGADO_DEPT FOREIGN KEY (DEPTID) REFERENCES DEPARTAMENTO)
                                          *
ERRO na linha 5:
ORA-00904: "DEPTID": identificador inválido 


SQL> CREATE TABLE EMPREGADO(
  2  ID_EMRPEGADO NUMBER(7),
  3  NOME VARCHAR(25),
  4  SOBRENOME VARCHAR(25),
  5  DEPTID NUMBER(7),
  6  CONSTRAINT FK_EMPREGADO_DEPT FOREIGN KEY (DEPTID) REFERENCES DEPARTAMENTO);

Tabela criada.

SQL> DROP TABLE EMPREGADO;

Tabela eliminada.

SQL> SELECT * FROM USER_RECYCLEBIN;

não há linhas selecionadas

SQL> SPOOL OFF
